# WeatherApp

## Меня зовут Владимир

## мой ник BRIT47 мой телеграмм brit_front

Работа с риложением

- для запуска приложения используйте **live server**
- используются svg sprite - не пугайтесь его в html
- в папке publiс скачены svg для будущего
- папка style разделена на компоненты, чтобы в будущем с удобством переездать на React или компонентный подход
- папка scripts также сделана для будущего, имеются файлы которые будут скорее всего применяться в следующих спринтах(плохо вник в задание сначала,но удалять не стал)
- Мок данные находятся в файлу db.json
- класс thumb для ползунка манипулирует через width.Сделано для того чтобы через js манипулировать положением ползунка. BG акцента реализован через относительное положение родительского компонента.Через планируется - если давление( или что то другое) , бэкграунд начинает растягиваться от центра.Класс dangers.
- Карточки генерируются через js класса **Card** , и цепляются к id weatherCards после загрузки HTML в файле script.js
- Прогноз погоды генерируется с помощью класса Forecast.В store созданы моковые данные погоды и используются в главном скрипте
- для скрытия переполнения прогноза погоды применен метод с использованием mask-image
-

# ПРЕДЛАГАЙТЕ:

-

## Правила

- Все файлы и директории называй с маленькой буквы, а файлы компонентов — с большой.(Card,Forecast,Store так как это компоненты )
- Не используй в реализации проекта фреймворки и сторонние библиотеки (Bootstrap, UI Kit, Sass и т. д.).

# Задачи

Вынести метеоданные в переменную, в отдельный файл. Данные карточек на главной странице заполнить из этой переменной.

Для описания стилей использовать методологию БЭМ.

Важно: в рамках этого задания выполняется только верстка блока карточек с заполнением данных о погоде из переменной, функционал получения актуальных данных о погоде реализовывать не требуется.

- 3я таска

* сверстать нижний блок с прогнозом «на 24 часа» и «на 5 дней».
* Минимальная ширина экрана — 1280px.
* Настроить обработчик вкладок для кнопок «на 24 часа» и «на 5 дней» в новом блоке так, чтобы при клике на кнопку срабатывало соответствующее переключение вкладок с выводом данных о погоде в карточках. Карточки вкладок заполнить данными из макета.
* Данные должны храниться в отдельных переменных для прогноза на «на 24 часа» и «на 5 дней».
* При нажатии на ссылку в футере, открывается новая вкладка с URL
  https://preax.ru
* Для описания стилей использовать методологию БЭМ.
* в рамках текущей задачи обработчик кнопки прокрутки не настраивать, только верстка элементов.

- 4я таска

* Выполнить согласно макету адаптивную и резиновую верстку страницы для мобильной и планшетной версий.

Минимальная ширина экрана — 375 px, 768 px. и 1280 px. для мобильной, планшетной и десктопной версии соответственно.
\\

### Я устал адаптивить =(

# Дебаг

---

WeatherApp(1.2)-круг 1

### Критические

- @resp 1.Переменная с данными выделена, но по ТЗ: нужно заполнить карточки пользуясь этими данными. У тебя сейчас хардкод html

- @resp 2.Минимальная ширина экрана 1280px. При 1279 должен появиться горизонтальный скролл(Не выполнено условие предыдущей таски === крит )

### Не критические

- @resp 3.Обводка у кружка прогресбара не прозрачная. Положение thumb захаркожено. Его нужно двигать в зависимости от данных в переменной
- @valentine 4. Используются шрифт Raleway начертанием, 400 700
  в данном случае используется 100 и 700

# WeatherApp(1.3)-круг 1

### Критические

- @bread Не прогрузились свгшки на часовых и дневных прогнозах.+

### Указать в элементах слайдера тег time и атрибут datetime +

#### Не сделано по прошлому заданию полностью прозрачное окружение у кружка +

`mask-image: radial-gradient(
    ellipse 12px 12px at 89px center,
    #0000 6px,
    #000 0
  );`

### Не критические

- @bread Как по мне бэкграунд ну уж слишком низкого качества

#### Не макетное значение backdrop-filter, должно быть 25px, сейчас 18 +

#### Размер логотипа отличается от макета +

#### У карточек с прогрессбаром есть смещение у элементов

#### Цвет фона у карточек в макете rgba(245, 244, 244, 0.4), сейчас используется rgba(245, 244, 244, 0.3)

#### Левая кнопка слайдера должна быть в состоянии disabled и иметь соответсвующие стили

#### У двух кнопок одинаковый id="arrow"

- @kiril153bcs

#### Из наиболее явных багов можно выделить цвет текста внутри карточек прогноза. По макету он чёрный (#202020), но в работе он белый (#f5f4f4) +

#### Не на всех браузерах картинка в карточках прогноза отображается верно. Лучше всего прописывать путь, начиная с "./" В работе путь выглядит так:+

../../public/images/weaterIcon/cloudly.svg

Но лучше будет сделать следующим образом:
./public/images/weaterIcon/cloudly.svg

#### И снова по поисковой строке. Точнее её стилям. По сути в компонентах у поисковой строки два стиля: при наведении и при вводимом тексте (active и typing согласно макету) один цвет бэкграунда — #DADADA, тогда как по умолчанию, при набранном тексте (filled) и при наведении, если текст набран (hover delete) — другой цвет, а именно #F5F4F466. И если в работе начать набирать текст, но убрать курсор с поисковой строки, то цвет у бэкграунда остаётся #F5F4F466, а #DADADA только при эффекте hover. В общем советую прописать стили для бэкраунда поисковой строки через +

:focus-within

. В таком случае при наборе текста будет использоваться на бэкграунде нужный цвет. Поправить состояния filled и hover delete тоже бы не помешало (чтобы был конкретный стиль и никакой hover его не перекрывал).

---

## Мой дебагинг

---

WeatherApp(1.2)

- 1. Карточки генерируются через js класса **Card** , и цепляются к id weatherCards после загрузки HTML в файле script.js
- 2. исправлено костылём overflow:hidden к боди больше 1280px и медиазапросом к боди overflow-x
- 3. исправлено - добавлено больше прозрачности
- 4. испровлено(к боди ошибочно был подключен font-weigth:100)

---

- баги с инпутом , у кого то не отображаются hover и иконки(исправлено - были проблемы с получением до элементов)

WeatherApp(1.3)

- 1. Решил проблему с путями для свг исконок слайдера и инпута
- 2.  Добавлены теги time и атрибут datetime сдайдера
- 3. скоректировал ренджи
- 4. backdrop-filter изменено значение на 25
- 5. расположение лого скоректировано
- 6. Скорректирован цвет элементов слайдера --color-font-slider-el:
- 7. Исправлены состояния инпута и добалена доступность через таб
